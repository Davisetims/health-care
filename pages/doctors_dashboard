<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Doctor Dashboard</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            display: flex;
            background-color: #f5f7fa;
        }

        .dashboard-container {
            display: flex;
            width: 100%;
        }

        .sidebar {
            width: 250px;
            background-color: #2d3748;
            color: white;
            padding: 20px;
            height: 100vh;
        }

        .sidebar h2 {
            margin-bottom: 20px;
        }

        .sidebar nav ul {
            list-style: none;
            padding: 0;
        }

        .sidebar nav ul li {
            padding: 10px 0;
        }

        .sidebar nav ul li a {
            color: white;
            text-decoration: none;
            display: block;
        }

        .sidebar nav ul li a.active {
            font-weight: bold;
        }

        .dashboard-content {
            flex: 1;
            padding: 20px;
        }

        header {
            display: flex;
            justify-content: space-between;
            background: white;
            padding: 10px;
            border-radius: 8px;
            margin-bottom: 20px;
        }

        .stats {
            display: flex;
            gap: 15px;
        }

        .stat-box {
            background: white;
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
            flex: 1;
            text-align: center;
            font-weight: bold;
        }

        .appointments, .quick-actions {
            background: white;
            padding: 20px;
            border-radius: 8px;
            margin-top: 20px;
        }

        .appointment, .quick-actions button {
            display: flex;
            justify-content: space-between;
            background: #eef2f6;
            padding: 10px;
            border-radius: 5px;
            margin-top: 10px;
        }

        .quick-actions button {
            background: #3182ce;
            color: white;
            border: none;
            padding: 10px;
            cursor: pointer;
        }
    </style>
</head>
<body>
    <div class="dashboard-container">
        <aside class="sidebar">
            <h2>HMS</h2>
            <nav>
                <ul>
                    <li><a href="#" class="active">Dashboard</a></li>
                    <li><a href="#">Appointments</a></li>
                    <li><a href="#">Patients</a></li>
                    <li><a href="#">Medical Records</a></li>
                    <li><a href="#">Prescriptions</a></li>
                    <li><a href="#">Settings</a></li>
                </ul>
            </nav>
        </aside>

        <main class="dashboard-content">
            <header>
                <input type="text" id="doctor-search" placeholder="Search...">
                <div class="user-info">Dr. John Doe</div>
            </header>

            <section class="stats">
                <div class="stat-box">2 Upcoming Appointments</div>
                <div class="stat-box">5 Active Patients</div>
                <div class="stat-box">3 Pending Prescriptions</div>
            </section>

            <section class="appointments">
                <h3>Upcoming Appointments</h3>
                <div id="doctor-list"></div>
            </section>

            <section class="quick-actions">
                <h3>Quick Actions</h3>
                <button>Schedule Appointment</button>
                <button>View Patients</button>
                <button>Prescriptions</button>
            </section>
        </main>
    </div>

    <script>
        document.addEventListener('DOMContentLoaded', async () => {
            const doctorListContainer = document.getElementById('doctor-list');
            const searchInput = document.getElementById('doctor-search');

            async function fetchDoctors() {
                try {
                    const response = await fetch('https://hcms-api-production.up.railway.app/api/doctors/');
                    const doctors = await response.json();
                    displayDoctors(doctors);
                } catch (error) {
                    console.error('Error fetching doctor data:', error);
                    doctorListContainer.innerHTML = '<p>Error loading doctor information.</p>';
                }
            }

            function displayDoctors(doctors) {
                doctorListContainer.innerHTML = '';
                doctors.forEach(doctor => {
                    const doctorCard = document.createElement('div');
                    doctorCard.className = 'appointment';
                    doctorCard.innerHTML = `
                        <h3>${doctor.name}</h3>
                        <p>Specialization: ${doctor.specialization}</p>
                        <p>Contact: ${doctor.contact}</p>
                        <p>Available Days: ${doctor.available_days}</p>
                    `;
                    doctorListContainer.appendChild(doctorCard);
                });
            }

            searchInput.addEventListener('input', () => {
                const query = searchInput.value.toLowerCase();
                const doctorCards = document.querySelectorAll('.appointment');
                doctorCards.forEach(card => {
                    const doctorName = card.querySelector('h3').textContent.toLowerCase();
                    card.style.display = doctorName.includes(query) ? 'block' : 'none';
                });
            });

            fetchDoctors();
        });
    </script>
</body>
</html>
